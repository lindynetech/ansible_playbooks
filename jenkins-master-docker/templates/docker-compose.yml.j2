---
version: "3.7"

services:
  {{ project_name }}: 
    image: "{{ image }}"    
    ports:
      - "8080:8080"
      - "50000:50000"
    volumes:
      - ./{{ jenkins_home }}:/var/jenkins_home
    restart: always